export function xf_formatBytes(bytes){if(bytes===null||bytes===undefined)return"";if(bytes===0)return"0 Bytes";const k=1024;const sizes=["Bytes","KiB","MiB","GiB"];const i=Math.floor(Math.log(bytes)/Math.log(k));return parseFloat((bytes/Math.pow(k,i)).toFixed(2))+" "+sizes[i]}export function xf_concatChunks(chunks){const totalLength=chunks.reduce(((acc,chunk)=>acc+chunk.length),0);const result=new Uint8Array(totalLength);let offset=0;for(const chunk of chunks){result.set(chunk,offset);offset+=chunk.length}return result}export function xf_calculateChecksum(str){let checksum=0;for(let i=0;i<str.length;i++){checksum=(checksum<<5)-checksum+str.charCodeAt(i);checksum|=0}return checksum}export function xf_readLocalStorage(key){const value=localStorage.getItem(key);console.log(`工具：读取本地存储：键：${key}：读到值：${value}`);return value}export function xf_writeLocalStorage(key,value){console.log(`工具：写入本地存储：键：${key}，值：${value}`);localStorage.setItem(key,value)}export function isMultipleOfTen(num){if(typeof num!=="number"||isNaN(num)){return false}return num%10===0}