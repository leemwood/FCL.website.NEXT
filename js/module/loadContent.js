import utils from"/js/module/utils.js";export default class loadContent{static abortController=null;static xf_abortAllLoadings(){if(this.abortController){this.abortController.abort();this.abortController=null;console.log("强行终止加载：已终止所有正在进行的请求。")}}static async xf_getHtmlContent(url,onProgress){console.log(`获取 HTML 内容：${url}`);this.abortController=new AbortController;return fetch(url,{signal:this.abortController.signal}).then((response=>{if(!response.ok){throw new Error(`获取 HTML 内容：出错：HTTP：${response.status}`)}const contentLength=response.headers.get("Content-Length");let loaded=0;if(!onProgress){return response.text()}const reader=response.body.getReader();const chunks=[];const total=contentLength?parseInt(contentLength,10):null;return new Promise(((resolve,reject)=>{function read(){reader.read().then((({done:done,value:value})=>{if(done){const html=(new TextDecoder).decode(utils.xf_concatChunks(chunks));resolve(html);return}chunks.push(value);loaded+=value.length;onProgress(loaded,total);read()})).catch(reject)}read()}))})).then((html=>{const parser=new DOMParser;const doc=parser.parseFromString(html,"text/html");return doc.body.firstElementChild})).catch((e=>{console.error("获取 HTML 内容：出错：",e);const errorElement=document.createElement("span");const errorInfo=utils.xf_handleError(e,{defaultMessage:`获取 HTML 内容：出错：${e.message}`});errorElement.textContent=errorInfo.message;errorElement.style.color=errorInfo.color;return errorElement}))}static xf_loadHtmlContent(htmlContent,container){console.log(`加载 HTML 内容到指定容器：容器：${container.id}`);try{while(container.firstChild){container.firstChild.remove()}container.appendChild(htmlContent)}catch(e){console.error("加载 HTML 内容到指定容器：出错：",e);const errorElement=document.createElement("span");errorElement.textContent=`加载 HTML 内容到指定容器：出错：${e.message}`;container.appendChild(errorElement)}}static async xf_loadHtmlContentFromUrl(url,container){console.log(`获取并加载 HTML 内容到指定容器：内容：${url}；容器：${container.id}`);try{const loadingElement=document.createElement("div");loadingElement.className="xf-loading";loadingElement.id="xf-loading-progress";loadingElement.innerHTML="正在加载<br>?%<br>0 / ?";container.innerHTML="";container.appendChild(loadingElement);const htmlContent=await this.xf_getHtmlContent(url,((loaded,total)=>{const percent=total?Math.round(loaded/total*100):0;const loadedFormatted=utils.xf_formatBytes(loaded);const totalFormatted=total?utils.xf_formatBytes(total):"?";const progressElement=document.getElementById("xf-loading-progress");if(progressElement){progressElement.innerHTML=`正在加载<br>${percent}%<br>${loadedFormatted} / ${totalFormatted}`}}));if(htmlContent){this.xf_loadHtmlContent(htmlContent,container)}mdui.mutation()}catch(e){console.error("获取并加载 HTML 内容到指定容器：",e);const errorInfo=utils.xf_handleError(e,{defaultMessage:`获取并加载 HTML 内容到指定容器：出错：${e.message}`});container.innerHTML=`<div class="xf-error-text" style="color: ${errorInfo.color};">${errorInfo.message}</div>`}}static async fetchItems(source,responseType="json"){if(typeof source==="string"){try{this.abortController=new AbortController;const response=await fetch(source,{signal:this.abortController.signal});if(!response.ok){throw new Error(`HTTP ${response.status}: ${response.statusText}`)}return await response[responseType]()}catch(error){if(error.name==="TypeError"){throw new Error(`网络错误：${error.message}`)}const errorInfo=utils.xf_handleError(error,{defaultMessage:error.message,isUserFriendly:false});if(errorInfo.isAbort){throw new Error("强行终止加载。")}throw error}}return source}}